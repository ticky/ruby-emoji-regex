language: ruby
before_install:
  - curl -o- -L https://yarnpkg.com/install.sh | bash -s
  - export PATH="$HOME/.yarn/bin:$PATH"
install: bin/setup
cache:
  bundler: true
  directories:
    - $HOME/.cache/yarn
